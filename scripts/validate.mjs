import fs from 'node:fs';import path from 'node:path';const root=new URL('../src/content/docs/',import.meta.url);let n=0;function visit(dir){for(const f of fs.readdirSync(dir,{withFileTypes:true})){const p=path.join(dir,f.name);if(f.isDirectory())visit(p);else if(/\.mdx?$/.test(f.name)){const text=fs.readFileSync(p,'utf8');if(!/^---\ntitle: .+\n/m.test(text))throw Error('Missing title: '+p);if(/(?:sk-[A-Za-z0-9]{20,}|ghp_[A-Za-z0-9]+|BEGIN (?:RSA |OPENSSH )?PRIVATE KEY)/.test(text))throw Error('Possible secret in documentation');n++;}}}visit(root.pathname);if(n<8)throw Error('Required guides missing');console.log(n+' documentation pages checked');
